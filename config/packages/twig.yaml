twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes: ['form.html.twig']
    globals:
        IP_SEDONA: 141.95.213.230
        contact_email: '%env(REPLY_TO_EMAIL)%'
