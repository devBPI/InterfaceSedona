Imperssion Search PDF
---------------------
Résultats de recherche
---------------------

{{ 'bloc.autority-result-information.your-references-autority'| trans }}
=====================
{% for element in printNoticeWrapper.noticeAuthority %}
    {{ loop.index }}) {{ element.title }}
    {% include "authority/print-detail.format.twig" with {'notice': element, 'format':'txt'} %}
{% endfor %}

{% for element in printNoticeWrapper.noticeIndice %}
    {{ loop.index }}) {{ element.title }}
    {% include "indice/print-detail.format.twig" with {'notice': element, 'format': 'txt'} %}
{% endfor %}


{{ 'bloc.autority-result-information.your-references-bibliographic'| trans }}
=====================
{% for element in printNoticeWrapper.noticeOnShelves %}
    {{ loop.index }}) {{ element.title }}
    {% include "notice/print-detail.format.twig" with {'notice': element, 'format': 'txt'} %}
{% endfor %}

{% for element in printNoticeWrapper.noticeOnLine %}
    {{ loop.index }}) {{ element.title }}
    {% include "notice/print-detail.format.twig" with {'notice': element, 'format': 'txt'} %}
{% endfor %}
