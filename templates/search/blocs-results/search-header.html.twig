<!-- Début - Bloc 14 Tri et pages -->

<div class="col-sm">
	<div class="dropdown mt-3">
		<span class="sr-only" aria-label="{{ ('bloc.search-header-footer.header.order-aria')|trans }}">{{ ('bloc.search-header-footer.header.order')|trans }}</span>
		<button class="dropdown-toggle" type="button" id="{{ ('bloc.search-header-footer.header.order-aria')|trans }}" data-toggle="dropdown" aria-haspopup="listbox" aria-expanded="false">
			{{ 'bloc.search-header-footer.header.order-choice'|trans }}
		</button>
		<ul class="dropdown-menu" aria-labelledby="{{ ('bloc.search-header-footer.header.order-aria')|trans }}" role="listbox" tabindex="-1">
            {% for index, value in constant('App\\Model\\Search\\SearchQuery::SORT') %}
                <li class="dropdown-item" role="option">
                    <a href="{{ add_sorting_to_uri(value) }}">{{ ('bloc.header-docs.order-'~ index) |trans }}</a>
                </li>
            {% endfor %}
		</ul>
	</div>
</div>

<div class="col-sm">
	<div class="dropdown text-sm-right mt-3">
		<span class="sr-only" aria-label="{{ ('bloc.search-header-footer.header.nb-result-aria')|trans }}">{{ 'bloc.search-header-footer.header.nb-result-title'|trans }}</span>
		<button class="dropdown-toggle" type="button" id="{{ ('bloc.search-header-footer.header.nb-result-aria')|trans }}" data-toggle="dropdown" aria-haspopup="listbox" aria-expanded="false">
		{{ 'bloc.search-header-footer.header.nb-result-by-col'|trans({'%nb%': 10}) }}
		</button>
		<ul class="dropdown-menu dropdown-menu-right" aria-labelledby="{{ ('bloc.search-header-footer.header.nb-result-aria')|trans }}" role="listbox" tabindex="-1">
            {% for count in [10, 25, 50] %}
                <li class="dropdown-item" role="option">
                    <a href="{{ add_rows_to_uri(count) }}" {% if objSearch.results.rows == count %}class="text-bold" {% endif %}>
                        {{ 'bloc.search-header-footer.header.nb-result-by-col'|trans({'%nb%': count }) }}
                    </a>
                </li>
            {% endfor %}
		</ul>
	</div>
</div>
<div class="col-12">
	<hr class="border-default mb-0">
</div>

<!-- Fin - Bloc 14 Tri et pages -->
