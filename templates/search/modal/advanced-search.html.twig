<!-- Début - Modal 13 recheche avancé -->
<div role="dialog" aria-modal="true" aria-labelledby="modal-search-advanced-title" class="modal fade modal--filter"
     id="modal-search-advanced">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="n1 modal-header__title" id="modal-search-advanced-title">
                    {{ 'modal.advanced-search.title'|trans }}
                </h1>
                <button class="close" type="button" aria-label="{{ 'global.modal-close'|trans }}" data-dismiss="modal">
                    <span aria-hidden="true" class="sr-only">X</span>
                    <img src="{{ asset('images/icones/icone-croix-blanc.svg') }}" alt=""/>
                </button>
            </div>

            <form action="{{ path('advanced_search') }}" method="get">

                {{ render(controller('App\\Controller\\SearchController::advancedSearchContent', {'request': app.request})) }}

                <div class="modal-footer">
                    <button type="button" class="btn-link btn">
                        {{ 'modal.advanced-search.clear-search'|trans }}
                    </button>
                    <button type="submit" aria-label="{{ 'modal.advanced-search.submit-aria'|trans }}"
                            name="action" class="btn btn-lg btn-primary btn-img">
                        <span>{{ 'modal.advanced-search.submit'|trans }}</span>
                        <img src="{{ asset('images/icones/icone-loupe.svg') }}" alt="" class="icone-loupe"/>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
<!-- Fin - Modal 13 recheche avancé -->
